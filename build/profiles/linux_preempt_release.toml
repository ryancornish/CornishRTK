# Release build of the preempt port
[profile]
name = "linux_preempt_release"
toolchain = "../toolchains/gcc-release.toml"
config_header = "../configs/sim.hpp"

[layout]
source_root = "../../src"
output_root = "../../out"

[components]
port = "linux_preempt"
time_driver = "simulation"

[features]
enable = [
   "round_robin",
   "sync",
]

[output]
archive = "libcyros.a"
